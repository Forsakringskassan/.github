---
# PUBLIC API -- DEPRECATED.
# Use bundle-app-npm-ci.yaml with `runtime-env: true` instead.
# Kept so that repositories can migrate on their own schedule.
name: Bundle App NPM CI with runtime-env (deprecated)

on:
  workflow_call:
    secrets:
      PACKAGES_RW_ACTOR:
        required: true
      PACKAGES_RW_TOKEN:
        required: true

jobs:
  call-bundle-app-npm-ci:
    uses: Forsakringskassan/.github/.github/workflows/bundle-app-npm-ci.yaml@main
    with:
      runtime-env: true
    secrets:
      PACKAGES_RW_ACTOR: ${{ secrets.PACKAGES_RW_ACTOR }}
      PACKAGES_RW_TOKEN: ${{ secrets.PACKAGES_RW_TOKEN }}
